- name: install nginx
  ansible.builtin.yum:
    name: nginx
    state: installed

- name: start nginx
  ansible.builtin.systemd:
      name: nginx
      state: started
      enabled: yes

- name: remove old directory
  ansible.builtin.file:
    path: /usr/share/nginx/html/
    state: absent



